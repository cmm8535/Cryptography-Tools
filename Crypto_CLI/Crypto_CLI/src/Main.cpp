#include <iostream>
#include "buildinfo.h"

int main(int, char* [])
{
      printf("Just a test to make sure I set up the project correctly (for the most part).\n");
      printf("%s\n",timestamp);
      return 0;
}